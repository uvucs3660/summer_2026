name: course_builder
description: Generate a Canvas Common Cartridge zip for CS 3660 from structured YAML+Markdown source.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  yaml: ^3.1.2
  markdown: ^7.1.1
  archive: ^3.4.10
  xml: ^6.5.0
  path: ^1.9.0
  crypto: ^3.0.3
  json_schema: ^5.1.7

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
